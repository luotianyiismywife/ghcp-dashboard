# 扩展清单 — `package.json`

对应源码文件：`package.json`

## 基础信息

| 字段 | 值 |
|------|-----|
| `name` | `ghcp-dashboard` |
| `displayName` | GitHub Copilot Insights Dashboard |
| `version` | 2.0.4 |
| `publisher` | `luotianyiismywife` |
| `license` | MIT |
| `icon` | `media/GHCP_dash_icon_circle.png` |

## 引擎要求

| 引擎 | 版本 |
|------|------|
| `vscode` | `^1.95.0` |

## 关键字段

| 字段 | 值 | 说明 |
|------|-----|------|
| `main` | `./src/extension.js` | 扩展入口 |
| `l10n` | `./l10n` | **必须**，告知 VS Code 翻译包路径 |
| `activationEvents` | `[]` | 延迟加载 |

## 贡献点

### commands（3 个）

| 命令 ID | 标题 |
|---------|------|
| `ghcpDashboard.open` | GHCP: Open Copilot Insights Dashboard |
| `ghcpDashboard.refresh` | GHCP: Refresh Account Data |
| `ghcpDashboard.switchAccount` | GHCP: Switch GitHub Account |

### viewsContainers（1 个）

`ghcp-dashboard` — 活动栏 GitHub 图标容器

### views（1 个）

`ghcpDashboard.sidebarView` — 侧边栏 WebView

### menus（1 个）

侧边栏标题栏刷新按钮

## 依赖

| 包 | 类型 |
|---|------|
| `@types/vscode` ^1.95.0 | devDependencies |
| `@vscode/vsce` ^2.22.0 | devDependencies |

**无运行时依赖。**
